# 行尾规范化。
#
# 为什么需要这个文件：
#   本移植版的源码来自 Windows 版 Electron 应用，部分文件是 CRLF 行尾。
#   不做规范化时会出现两类问题：
#     1. 编辑工具在保存时悄悄把 CRLF 转成 LF（或反之），diff 显示"几乎每一行都改了"，
#        真实改动只有一两处 —— 审阅者无法判断改了什么。
#     2. 不同机器 clone 后工作区行尾不一致，同一段代码在不同人那里 diff 结果不同。
#
# 策略：
#   · 默认按文本处理，仓库内统一以 LF 存储
#   · 明确指定 Windows 下需要 CRLF 的文件类型（批处理/命令脚本）
#   · 二进制文件不做任何转换

# 默认：自动识别文本，仓库内统一 LF
* text=auto

# 明确的文本文件
*.js      text
*.mjs     text
*.cjs     text
*.json    text
*.md      text
*.sh      text eol=lf
*.py      text eol=lf
*.yml     text
*.yaml    text
*.ini     text
*.css     text
*.html    text

# Windows 专用脚本保留 CRLF（否则 cmd.exe 解析会出问题）
*.bat     text eol=crlf
*.cmd     text eol=crlf

# 二进制文件：禁止任何转换
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.ico     binary
*.svg     text
*.node    binary
*.so      binary
*.dll     binary
*.exe     binary
*.db      binary
*.asar    binary
*.AppImage binary
*.deb     binary
*.rpm     binary

# 语言文件与大型数据
*.pak     binary
*.dat     binary
*.bin     binary
