{
  "id": "memory-growth",
  "name": "记忆库自增长",
  "version": "1.0.0",
  "apiVersion": 1,
  "category": "knowledge",
  "description": "记忆库自增长：定时抽取（事件/梗/关系三类）→ 去重/相似合并 → 冲突标记 → 自动/人工审核 → 检索注入 → 生命周期（撤销/遗忘/归档/捞回）。MongoDB 软依赖：无库时不工作、优雅降级不卡聊天；语义向量（3917）软依赖，不可用退回哈希向量。默认只读（inject=false 不参与对话）。",
  "enabledByDefault": false,
  "capabilities": [
    "memory.recall",
    "memory.extraction",
    "memory.growth-status",
    "memory.review",
    "memory.cleanup",
    "memory.lifecycle",
    "action.memory",
    "panel.memory"
  ],
  "requires": [],
  "settings": {
    "mongoUri": "mongodb://127.0.0.1:27017",
    "mongoDb": "qq_agent_memory",
    "extractionIntervalMs": 600000,
    "inject": false,
    "minScore": 0.48,
    "simVector": 0.95,
    "simSuspect": 0.88,
    "reReviewAfter": 3,
    "archiveAfterDays": 0,
    "autoApproveMinConfidence": 0.7,
    "semanticEmbedEnabled": true,
    "semanticEmbedUrl": "http://127.0.0.1:3917",
    "weeklyCleanup": false,
    "cleanupIntervalMs": 604800000,
    "cleanupDays": 90,
    "cleanupMaxMention": 1,
    "pinnedProtected": true
  },
  "configSchema": {
    "mongoUri": { "type": "string", "label": "MongoDB 连接串", "description": "记忆库独立库 qq_agent_memory，与知识库物理隔离。连不上时记忆库不工作（降级跳过），不卡聊天。" },
    "mongoDb": { "type": "string", "label": "库名", "description": "默认 qq_agent_memory。" },
    "extractionIntervalMs": { "type": "number", "label": "抽取间隔 (ms)", "description": "默认 600000（10 分钟），仅在有新消息时调模型。" },
    "inject": { "type": "boolean", "label": "注入对话", "description": "默认关闭（只读）。开启后检索到的记忆会拼进系统提示词。" },
    "minScore": { "type": "number", "label": "检索分数门槛", "description": "默认 0.48（语义空间实测：噪声上限 0.46、命中下界 0.51）。低于不过滤。" },
    "simVector": { "type": "number", "label": "自动合并向量阈值", "description": "默认 0.95（语义空间，只看向量）。达到即自动并进已有条目。" },
    "simSuspect": { "type": "number", "label": "存疑标记阈值", "description": "默认 0.88。最高分达此值但不达自动合并 → 打 suspect_dup 交人裁决。" },
    "reReviewAfter": { "type": "number", "label": "拒绝重提审次数", "description": "被拒绝的记忆被提到 ≥ 此次数（默认 3）时带 reopened 标记重提审。0 = 关闭。" },
    "archiveAfterDays": { "type": "number", "label": "自动归档天数", "description": "默认 0（关）。久没提起的旧事件沉入 archived。自动归档默认关 —— 让机器自己藏老记忆得由人点头。" },
    "autoApproveMinConfidence": { "type": "number", "label": "自动审批置信度", "description": "默认 0.7。低于留待审。" },
    "semanticEmbedEnabled": { "type": "boolean", "label": "启用语义向量", "description": "开启后探测 3917 端口 ONNX 语义服务；不可用自动退回哈希向量。" },
    "semanticEmbedUrl": { "type": "string", "label": "语义向量服务地址", "description": "默认 http://127.0.0.1:3917。" },
    "weeklyCleanup": { "type": "boolean", "label": "定期清理冷记忆", "description": "开启后按下面的门槛把很久没被提到的记忆归档（不是删除，可 unarchive 找回）。默认关。" },
    "cleanupIntervalMs": { "type": "number", "label": "清理周期 (ms)", "description": "默认 604800000（7 天）。" },
    "cleanupDays": { "type": "integer", "label": "冷多久算冷（天）", "description": "超过这么多天没被提到才考虑归档。默认 90。" },
    "cleanupMaxMention": { "type": "integer", "label": "被提及次数门槛", "description": "提及次数 ≤ 这个值才算冷门。默认 1（只提过一次的）。" },
    "pinnedProtected": { "type": "boolean", "label": "固定项不清理", "description": "开启了固定（memory_pin）的记忆永不被自动归档。默认开。" }
  },
  "prompt": null
}
